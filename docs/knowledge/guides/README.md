# Developer Guides

How to do something as a contributor: set up an environment, run the build,
publish a release, avoid the platform traps that only show up on one target.
Written for someone doing the task for the first time.

Not here: instructions addressed to AI agents — those live in `AGENTS.md` at
the repo root, and the tracker workflow lives in `docs/WORKFLOW.md`.
