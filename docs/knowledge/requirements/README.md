# Product Requirements

What a capability must do and who needs it, written as a narrative, plus a
`## Stories` table of the tracker issues that deliver it.

**Acceptance criteria never appear here.** AC belongs on the story, where it
can be checked off; a PRD that carries its own checklist immediately drifts
from the tracker. Link the issue instead.
